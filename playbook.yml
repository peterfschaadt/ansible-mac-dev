---
- hosts: mac
  connection: local
  user: {{ username }}

# Roles
# - General
# - Osxlockdown
# - Python
# - Ruby
# - Node
# - Homebrew
# - Git
# - Mackup
# - Sublime Text
# - Zsh
# - Zprezto

  tasks:

#
### Symlinks
#

    - name: Create home directory symlinks
      file: src={{ item.from }} dest={{ item.to }} state=link
      sudo: True
      with_items:
        - from: ~/Dropbox/mac/bin
          to: ~/bin

# TODO: Configure Dotfiles (osxdefaults)

# TODO: Configure Zprezto (https://github.com/sorin-ionescu/prezto)
